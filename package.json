{
  "devDependencies": {
    "husky": "^8.0.2"
  },
  "scripts": {
    "prepare": "husky install",
    "lint": "cd backend && yarn run lint --fix && cd ../frontend && yarn run lint --fix",
    "test": "cd backend && yarn run test && cd ../frontend && yarn run test"
  },
  "license": "MIT",
  "private": true
}