import React from 'react';

const FrontPage = () => {
    console.log(Object.keys(window));
    return (
        <div>
            <h1>Front Page</h1>
        </div>
    );
};

export default FrontPage;