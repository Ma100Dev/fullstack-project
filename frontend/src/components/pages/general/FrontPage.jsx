// TODO: Add proper front page
// TODO: Make app look better

import PageSelector from '@components/reusables/PageSelector';

const FrontPage = () => {
    return (
        <div>
            <h1>Front Page</h1>
            <PageSelector />
        </div>
    );
};

export default FrontPage;
